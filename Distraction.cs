using UnityEngine;

namespace SDG.Unturned
{
    // Upon being instatiated waits 2.5 seconds and then creates a zombie distraction with a radius of 24 meters.
    // Destroys itself after first use, cannot be reset.
    public class Distraction : MonoBehaviour
    {
    }
}
