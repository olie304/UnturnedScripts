using UnityEngine;

namespace SDG.Framework.Devkit
{
    // Requires a BoxCollider on the base Game Object
    // Presumably prevents zombies from passing through the BoxCollider?
    // Game Object Layer is set to 22 at runtime and cannot be overriden
    public class NavClipVolume : MonoBehaviour
    {
    }
}
